<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-2">
  <div class="container-fluid px-4">

    <a class="navbar-brand fw-bold text-primary" href="/products">
      Shopping App
    </a>


    <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-3">
      <li>
        <a class="nav-link fw-semibold active" href="/products">Home</a>
      </li>
      <% if(currentUser && currentUser.role==='seller' ){ %>
        <li class="nav-item">
          <a class="nav-link" href="/product/new">New</a>
        </li>
        <% } %>
    </ul>

    <div class="d-flex align-items-center gap-3">

      <% if(!currentUser){ %>
        <a href="/login" class="btn btn-outline-primary btn-sm px-3">Login</a>
        <% } else { %>
          <span class="text-muted small">
            Hello, <strong>
              <%= currentUser.username %>
            </strong>
          </span>
          <a href="/logout" class="btn btn-outline-danger btn-sm px-3">Logout</a>
          <% } %>

    </div>
  </div>
</nav>